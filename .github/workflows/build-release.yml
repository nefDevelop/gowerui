name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'ubuntu-22.04'
            target: 'x86_64-unknown-linux-gnu'
            binary_name: 'gower'
            sidecar_name: 'gower-x86_64-unknown-linux-gnu'
          - platform: 'windows-latest'
            target: 'x86_64-pc-windows-msvc'
            binary_name: 'gower.exe'
            sidecar_name: 'gower-x86_64-pc-windows-msvc.exe'

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      - name: install frontend dependencies
        run: npm install

      - name: fetch sidecar binary from nefDevelop/gower
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p src-tauri/binaries
          # Fetch the latest release info from GitHub API using the token
          REPO="nefDevelop/gower"
          LATEST_RELEASE_JSON=$(curl -s -H "Authorization: token $GH_TOKEN" https://api.github.com/repos/$REPO/releases/latest)
          
          # Check if we got a valid JSON response with a tag_name
          GOWER_VERSION=$(echo "$LATEST_RELEASE_JSON" | grep '"tag_name":' | head -n 1 | cut -d '"' -f 4)
          
          if [[ -z "$GOWER_VERSION" ]]; then
            echo "Error: Could not find latest version for $REPO"
            echo "API Response: $LATEST_RELEASE_JSON"
            exit 1
          fi
          
          if [[ "${{ matrix.platform }}" == "ubuntu-22.04" ]]; then
            # Download Linux binary named 'gower'
            DOWNLOAD_URL=$(echo "$LATEST_RELEASE_JSON" | grep "browser_download_url" | grep "/gower\"" | head -n 1 | cut -d '"' -f 4)
          else
            # Download Windows binary named 'gower.exe'
            DOWNLOAD_URL=$(echo "$LATEST_RELEASE_JSON" | grep "browser_download_url" | grep "/gower.exe\"" | head -n 1 | cut -d '"' -f 4)
          fi
          
          if [[ -z "$DOWNLOAD_URL" ]]; then
            echo "Error: Could not find download URL for version $GOWER_VERSION"
            exit 1
          fi
          
          echo "Downloading sidecar version $GOWER_VERSION from $DOWNLOAD_URL"
          curl -L -H "Authorization: token $GH_TOKEN" "$DOWNLOAD_URL" -o src-tauri/binaries/${{ matrix.sidecar_name }}
          chmod +x src-tauri/binaries/${{ matrix.sidecar_name }} || true
          
          # Create a metadata file for the app to know which sidecar version it uses
          # We place it in src/lib so the frontend can import it
          echo "{\"version\": \"$GOWER_VERSION\", \"repo\": \"$REPO\", \"binary\": \"${{ matrix.binary_name }}\"}" > src/lib/sidecar-info.json

      - name: build tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: 'gowerui v__VERSION__'
          releaseBody: 'Automatic release from GitHub Actions. Includes portable versions for better compatibility.'
          releaseDraft: true
          prerelease: false
          args: --target ${{ matrix.target }}

      - name: package portable (linux)
        if: matrix.platform == 'ubuntu-22.04'
        shell: bash
        run: |
          mkdir -p gowerui-linux-portable
          cp src-tauri/target/${{ matrix.target }}/release/gowerui gowerui-linux-portable/
          cp src-tauri/binaries/gower-x86_64-unknown-linux-gnu gowerui-linux-portable/gower
          chmod +x gowerui-linux-portable/*
          tar -czf gowerui-linux-portable.tar.gz gowerui-linux-portable/

      - name: package portable (windows)
        if: matrix.platform == 'windows-latest'
        shell: bash
        run: |
          mkdir -p gowerui-windows-portable
          cp src-tauri/target/${{ matrix.target }}/release/gowerui.exe gowerui-windows-portable/
          cp src-tauri/binaries/gower-x86_64-pc-windows-msvc.exe gowerui-windows-portable/gower.exe
          7z a gowerui-windows-portable.zip gowerui-windows-portable/

      - name: upload portable assets
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
        with:
          files: |
            gowerui-linux-portable.tar.gz
            gowerui-windows-portable.zip
          tag_name: v${{ steps.tauri_version.outputs.version }} # We'd need to extract version, but for simplicity let's use the draft tauri-action created
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
